apiVersion: v1
kind: Service
metadata:
  name: quakewatch-service
spec:
  selector:
    app: quakewatch
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
  type: NodePort